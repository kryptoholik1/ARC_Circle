# arca-demo-bounty
Tiny bounty repo. `calc.add` has a bug; the bounty is to make `pytest -q` green.
The Arca evaluator checks out the submitted commit, runs the tests, and releases
the USDC escrow automatically if they pass.
